/**
 * GoogleSearchPanel — Command Center widget.
 *
 * STUB: the original component was never committed, which broke the production
 * build. Rendering `null` keeps the Command Center page building and loading;
 * restore the real implementation when it's available.
 */
export default function GoogleSearchPanel() {
  return null;
}

/**
 * LeadMap — map visualization for the Lead Inbox.
 *
 * STUB: the original component was never committed, which broke the production
 * build. It accepts the same props and renders `null` so LeadInbox builds and
 * loads; restore the real map implementation when it's available.
 */
export default function LeadMap(/* { leads, jobs, estimates } */) {
  return null;
}
